# 数据安全与隐私保护筛选系统使用说明

项目已从单文件命令行脚本重构为 FastAPI + SQLite + React 的可视化工作台。

请使用新的入口：

```powershell
.\start.bat
```

旧的 `一键启动.bat` 也会转发到 `start.bat`。

完整功能、手动启动方式、腾讯文档 CSV 上传去重和 Markdown 导出说明见 `README.md`。
